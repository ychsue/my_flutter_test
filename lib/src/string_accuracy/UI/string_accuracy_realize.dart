import 'package:flutter/material.dart';

class StringAccuracyRealize extends StatelessWidget {
  const StringAccuracyRealize({Key? key}) : super(key: key);

// TODO
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Row(
            children: [],
          )
        ],
      ),
    );
  }
}
