import 'package:flutter/material.dart';

import 'n30_rotate_animate.dart';

class N30ValueListenableBuilderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: N30RotateAnimate(),
    );
  }
}
